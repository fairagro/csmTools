% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit_dssat_files.R
\name{edit_cultivar}
\alias{edit_cultivar}
\title{Edit or add a cultivar entry in a DSSAT genotype file}
\usage{
edit_cultivar(
  cname,
  model,
  ccode = NULL,
  ecode = NULL,
  expno = NULL,
  ppars = list(),
  gpars = list(),
  write = FALSE,
  ...
)
}
\arguments{
\item{cname}{Character. Name of cultivar to append or edit.}

\item{model}{Character. Four-letter DSSAT crop model code (e.g. \code{"MZCER"}),
used to locate the corresponding genotype files.}

\item{ccode}{Character or \code{NULL}. Cultivar identifier code (e.g. \code{"IB0001"}).
Defaults to \code{"XX9999"} with a warning if not supplied.}

\item{ecode}{Character or \code{NULL}. Ecotype code. Defaults to \code{"DFAULT"} If \code{NULL} or not found.}

\item{expno}{Character or \code{NULL}. Experiment number field. Defaults to \code{"."} if not supplied.}

\item{ppars}{Named list. Phenology (or other) cultivar parameters to set or override
e.g. \code{list(P1 = 200, P2 = 0.5)}.}

\item{gpars}{Named list. Growth cultivar parameters to set or override.}

\item{write}{Logical. If \code{TRUE}, writes the updated \code{.CUL} file to the DSSAT Genotype directory.
A backup of the original file is created under \verb{<dssat_dir>/0_DataBackUp/Genotype/} if one
does not already exist. Defaults to \code{FALSE}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A tibble containing the full updated cultivar tabl
}
\description{
Modifies an existing cultivar entry or adds a new one in the DSSAT \code{.CUL}
file associated with a given crop model. New cultivars are initialised with
parameter values equal to the median of the \code{MINIMA} and \code{MAXIMA} rows in
the file, which can then be overridden via \code{ppars} or \code{gpars}.
}
\details{
The function resolves the DSSAT installation directory via \code{.get_dssat_dir()} and expects
the \code{.CUL} file to exist at \verb{<dssat_dir>/Genotype/<model>048.CUL}.

When adding a \strong{new} cultivar (i.e. \code{cname} is not already in the file), default parameter
values are computed as the median of the upper and lower bounds set in the \code{.CUL} file.
Any values supplied via \code{ppars}/\code{gpars} overwrite these defaults.

When \code{write = TRUE}, the backup is only created once: subsequent calls will not overwrite
an existing backup.
}
\examples{
\dontrun{
# Modify an existing cultivar's parameters
edit_cultivar(
  cname = "MyVariety",
  model = "MZCER",
  ppars = list(P1 = 210, P2 = 0.4),
  write = FALSE
)

# Add a new cultivar entry and write to disk
edit_cultivar(
  cname  = "NewVar",
  model  = "MZCER",
  ccode  = "IB9999",
  ecode  = "LNDET",
  ppars  = list(P1 = 190),
  write  = TRUE
)
}

}
\seealso{
\code{\link[DSSAT:read_eco]{DSSAT::read_eco()}}, \code{read_cul2()}, \code{write_cul2()}
}
