% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_weather_data.R
\name{get_weather_data}
\alias{get_weather_data}
\title{Download, format, and impute daily weather time series}
\usage{
get_weather_data(lon, lat, from, to, pars, res, src, output_path = NULL)
}
\arguments{
\item{lon}{Numeric. Longitude of the target location in decimal degrees.}

\item{lat}{Numeric. Latitude of the target location in decimal degrees.}

\item{from}{Character or Date. Start date of the requested time range in
\code{"YYYY-MM-DD"} format (inclusive).}

\item{to}{Character or Date. End date of the requested time range in
\code{"YYYY-MM-DD"} format (inclusive).}

\item{pars}{Character vector. Target weather variables to retrieve. One or more of
\code{"air_temperature"}, \code{"precipitation"}, \code{"solar_radiation"}, \code{"dewpoint"},
\code{"relative_humidity"}, \code{"wind_speed"}, \code{"par"}, or \code{"evaporation"}.}

\item{res}{Character. Temporal resolution. One of \code{"daily"} or \code{"hourly"}.}

\item{src}{Character. Source repository. One of \code{"dwd"} or \code{"nasa_power"}.}

\item{output_path}{Character. Optional file path to save the output.}
}
\value{
A named list containing a \code{WEATHER_DAILY} data frame with the requested variables
for the specified location and time range.
}
\description{
Fetches weather data for a target location and time range from a supported source repository
and returns it as a named list of data frames.
}
\details{
Validates \code{src}, \code{pars}, and \code{res} against supported values, then dispatches to
the appropriate download handler. For \code{"nasa_power"}, each element of \code{pars} is mapped to its
corresponding NASA POWER parameter code and fetched via \code{nasapower::get_power()}.
The \code{"dwd"} source is currently a placeholder.
}
